import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent, hem AppRegistry'yi ayarlar hem de 
// çevresel uyumluluğu (web/mobil) sağlar.
registerRootComponent(App);